spring:
  datasource:
    username: staff
    password: password
    url: jdbc:postgresql://localhost:9001/staff
  jpa:
    database: DEFAULT
    hibernate.ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.sql