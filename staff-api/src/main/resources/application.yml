spring:
  datasource:
    username: staff
    password: password
    url: jdbc:postgresql://localhost:5433/staff
  security:
    basic:
      enabled: false
  jpa:
    database: DEFAULT
    hibernate.ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.sql
security:
  basic:
    enabled: false